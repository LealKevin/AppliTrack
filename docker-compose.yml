services:
  api:
    build: .
    ports:
      - "8000:8080"
    env_file: .env
    depends_on:
      - db

  db:
    image: postgres:latest
    restart: always
    ports:
      - "5434:5432"
    env_file:
      - .env.db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:


